#include <stdlib.h>

int main() {
	int x, y;
	long long int z;
	char w;
	float v;
	double u;
	// Complete the code.
	scanf("%d %d %lld %c %f %lf", &x, &y, &z, &w, &v, &u);
	printf("%d\n%d\n%lld\n%c\n%f\n%lf", x, y, z, w, v, u);

	return 0;
}